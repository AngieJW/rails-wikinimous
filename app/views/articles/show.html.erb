<div class="container"><div class="card">
  <div class="card-body">
  <h1 class="card-title"><%= @article.title %> </h1>

<p class="card-text"><%= @article.content %></p>
</div></div></div>
<div type="button" class="btn btn-info"><%= link_to "Back to list", articles_path %></div>
<div class="btn btn-warning"><%= link_to "Edit this article", edit_article_path %></div>
<div class="btn btn-danger"><%= link_to "Delete this article", article_path(@article),
  data: {turbo_method: :delete, turbo_confirm: "Delete #{@article.title}?"} %></div>
